---
description: 书籍与引文数据生成的通用规范，适用于 JSON 种子数据的新增、扩展和校验
globs:
  - backend/data/**
alwaysApply: false
---

> 本文档规范书籍数据（books.json）的生成与维护流程。所有通过 LLM 生成或人工补充的书籍数据必须遵循以下规则。

## 数据结构规范

1. **顶层数组** - JSON 文件为书籍对象数组，每本书包含 `title`（书名）、`author`（作者）、`sort_order`（排序）、`sentences`（句子数组）四个必填字段
2. **句子对象** - 每条句子包含以下必填字段：
   - `text`：原文金句（核心展示内容）
   - `context_before`：金句前的上下文（30-150 字），帮助读者理解语境
   - `context_after`：金句后的上下文（30-150 字），延伸金句的含义
   - `chapter`：所属章节名称，使用原书的章节标题
   - `similar_quotes`：共鸣引文数组（至少 1 条）
   - `opposite_quotes`：对立引文数组（至少 1 条）
   - `sort_order`：句子在书内的排序（从 0 开始连续递增）
3. **关联引文对象** - `similar_quotes` 和 `opposite_quotes` 中的每个对象包含：
   - `text`：引文原文
   - `book_title`：出处书名
   - `book_author`：出处作者
   - `sentence_id`（可选）：如果引文对应本数据集中已有的句子，填入其 UUID

## 内容真实性（最高优先级）

1. **原文忠实** - `text` 字段必须是该书的真实原文或其公认中文译本，禁止凭印象改写或编造；如果无法确认精确措辞，必须查证后再填入
2. **书名作者匹配** - 书名与作者必须严格对应，禁止张冠李戴；翻译人名使用该书中文版通行译法
3. **章节真实** - `chapter` 必须使用原书的真实章节名称或编号，禁止编造章节名
4. **上下文真实** - `context_before` 和 `context_after` 应忠实反映原书中该金句前后的内容，可适当精简但不可编造不存在的上下文
5. **关联引文出处真实** - `similar_quotes` 和 `opposite_quotes` 中的引文、书名、作者必须真实存在且匹配；禁止为凑数而编造看似合理的引文

## 选句标准

1. **一句封神** - 选择该书中最具穿透力的句子，读者看到一句就想读整本书；优先选观点鲜明、表达精炼、能引发思考的短句
2. **长度克制** - 单条金句控制在 15-80 字，超过 80 字的长段落必须精简到核心表达；金句是钩子不是摘要
3. **覆盖全书** - 句子应覆盖全书的核心章节，不要扎堆在某一章；通过 `chapter` 字段体现分布
4. **避免重复观点** - 同一本书的 20 条句子之间不应有高度相似的观点，每条应切入不同角度
5. **每书固定数量** - 每本书固定提取 20 条句子（sort_order 0-19），保持数据集均匀

## 上下文质量

1. **前文铺垫** - `context_before` 应为金句提供理解前提，让读者知道作者在什么语境下说出这句话；应是"因为..."而非重复金句本身
2. **后文延伸** - `context_after` 应展示金句之后作者的进一步论述或结论，给读者"继续读下去"的理由
3. **互不重复** - `context_before`、`text`、`context_after` 三者内容不应有信息重叠；如果金句本身已足够清晰，上下文应补充不同维度的信息
4. **语气连贯** - 三者读起来应像一段连贯的文章，不能出现逻辑断裂

## 关联引文质量

1. **共鸣引文** - `similar_quotes` 中的引文应与金句在思想层面形成共振：不同作者、不同时代、不同领域说了类似的话，让读者产生"英雄所见略同"的惊喜
2. **对立引文** - `opposite_quotes` 中的引文应与金句形成有意义的对话：不是简单否定，而是从另一个角度看同一问题，让读者思考"两边都有道理"
3. **跨领域优先** - 关联引文优先选择不同学科、不同文化背景的来源，避免在同一领域内自说自话
4. **避免自引** - 关联引文应来自不同于当前金句的作者，除非该书不同章节的观点之间存在自我矛盾的有趣对话
5. **引文独立可读** - 每条关联引文脱离上下文也能独立理解，不能是需要整段才能理解的半句话

## 数据一致性

1. **sort_order 连续** - 书的 `sort_order` 在数组中从 0 连续递增；句子的 `sort_order` 在每本书内从 0 递增到 19
2. **无空数组** - `similar_quotes` 和 `opposite_quotes` 不允许为空数组，每个至少包含 1 条引文
3. **字段完整** - 所有必填字段不允许缺失或为空字符串
4. **ID 稳定性** - 句子 ID 由服务层通过 `_stable_uuid("sentence", f"{book_id}|{text[:50]}")` 确定性生成，JSON 数据中不包含 ID 字段；修改金句 `text` 前 50 字符会导致 ID 变化，收藏关系断裂，需谨慎
5. **编码规范** - JSON 文件使用 UTF-8 编码，2 空格缩进，无尾逗号

## 选书标准

1. **思想密度** - 优先选择观点密集、金句产出率高的书籍；工具书、教材、纯叙事作品不适合
2. **跨学科覆盖** - 数据集整体应覆盖多个学科领域（哲学、历史、心理学、社会学、科技、文学等），避免集中在单一领域
3. **经典与现代平衡** - 既选经得起时间检验的经典，也选引发当代讨论的现代作品
4. **中文可得** - 优先选择有高质量中文译本的作品，确保引文的中文表达准确流畅

## 生成流程

1. **先校验再入库** - 新生成的书籍数据必须逐条校验真实性（书名-作者匹配、引文准确性、章节存在性），校验通过后才能合入 books.json
2. **增量追加** - 新书数据追加到数组末尾，`sort_order` 接续现有最大值 +1；不修改已有书籍的 sort_order
3. **交叉引用** - 如果新增引文恰好来自数据集中已有的书籍句子，在关联引文中填入 `sentence_id` 建立双向关联
4. **批量校验脚本** - 提交前应对整个 JSON 文件做结构校验：字段完整性、sort_order 连续性、数组非空、JSON 合法性
