---
description: 产品设计最佳实践，涵盖用户流程、信息架构、状态管理、数据流设计等通用规则
globs:
alwaysApply: true
---

> 本文档是产品设计的**参考惯例库**，不是强制清单。实现功能时应以用户的实际需求为准，从中选取适用的条目参考，未提及的需求不受本文档限制，已提及的条目也不意味着必须实现。

## 用户流程设计

1. **线性引导流程** - 核心功能采用分步骤线性引导，避免信息过载；每步只聚焦一个任务（如：启动页 → 信息收集 → 核心体验 → 结果展示）
2. **断点续传** - 用户中途退出后再次进入时，自动恢复到上次的进度位置；使用步骤映射表（如 `STEP_ROUTES`）管理用户进度
3. **渐进式披露** - 信息和功能按需展示，不一次性暴露所有内容；复杂流程拆分为可消化的小步骤
4. **明确的进度指示** - 多步骤流程必须提供进度指示器（如圆点、进度条、步骤编号），让用户知道当前位置和剩余步骤

## 信息架构

1. **集中式文案管理** - 所有用户可见文案统一放在 `data/texts` 或 `constants` 中管理，禁止在页面代码中硬编码文案
2. **配置驱动** - 环境配置（开发/测试/生产）严格分离，通过配置文件切换，不在代码中写死环境判断
3. **数据预加载** - 关键资源（图片、配置数据）在应用启动时预加载，减少用户等待；使用 `preload` 策略提前下载
4. **模块化数据** - 业务数据按领域拆分到独立文件（如 destinations、collection、texts），保持数据层清晰

## 状态管理

1. **最小状态原则** - 只存储必要的状态，可计算的值不存储；避免状态冗余和不一致
2. **本地持久化** - 用户关键数据（登录凭证、用户信息、进度）持久化到本地存储，支持离线访问和断点续传
3. **状态分层** - 组件内部状态用局部状态管理，跨组件共享状态通过事件通信或全局状态管理
4. **单一数据源** - 同一份数据只在一个地方维护，其他地方通过引用获取，避免数据不一致

## 登录与认证

1. **用户主动触发登录** - 小程序登录必须由用户主动操作触发（点击按钮、点击需要登录的功能），禁止在 `onLaunch`、`onLoad` 中自动静默登录；浏览类功能不要求登录，仅在需要写入操作（收藏、评论、支付）时引导登录
2. **标准微信授权登录页** - 登录页必须使用微信标准组件：`button open-type="chooseAvatar"` 获取头像、`input type="nickname"` 获取昵称；禁止用 `wx.showModal` 或自定义弹窗替代
3. **登录流程分两步** - 第一步 `wx.login` 换 code → 后端换 openid → 生成 JWT token；第二步用户在登录页填写头像和昵称 → 调用 `PUT /api/auth/me` 更新 profile；两步解耦，token 获取不依赖用户信息收集
4. **登录即注册** - 不设独立注册流程，首次登录时自动创建用户；通过 openid 判断新老用户
5. **JWT 无状态认证** - 使用 HS256 签名的 JWT Token，payload 包含 `sub`（user_id）、`exp`、`iat`；有效期 7 天
6. **Token 本地持久化** - 各端使用原生存储持久化 Token：小程序用 `wx.setStorageSync`，Web 用 `localStorage`，iOS 用 `UserDefaults`
7. **请求拦截器统一注入** - 前端请求封装层自动从本地存储读取 Token 并注入 `Authorization: Bearer` 头，业务代码无需手动传递
8. **401 自动登出** - 请求拦截器检测到 401 响应时，自动清除本地 Token 并跳转登录页
9. **认证分级依赖** - 后端使用 `CurrentUser`（必须登录）、`OptionalUser`（可选登录）、`AdminUser`（管理员）三级依赖注入，按接口敏感度选用
10. **账号注销** - 提供 `DELETE /api/auth/me` 接口支持用户主动注销，符合平台合规要求

## 点赞、收藏与互动

1. **Toggle 模式** - 点赞和收藏均使用单一接口 toggle 切换状态，前端调用同一个 API 实现操作/取消
2. **软取消** - 点赞和收藏记录使用 `is_cancelled` 布尔字段标记取消，不做物理删除，保留行为数据可追溯
3. **唯一约束防重复** - 点赞表和收藏表均使用 `(user_id, target_id)` 联合唯一约束，数据库层面防止重复
4. **计数缓存** - 被操作对象维护 `like_count`、`bookmark_count` 冗余字段，避免每次查询都做 COUNT 聚合
5. **多维度支持** - 点赞和收藏支持对不同实体类型操作（卡片、提供者等），每种类型独立建表，保持数据模型清晰
6. **收藏列表** - 用户可查看个人收藏列表，按收藏时间倒序排列，支持分页加载

## 分享

1. **平台原生分享** - 小程序使用 `onShareAppMessage` 和 `onShareTimeline` 实现分享到好友和朋友圈，纯客户端实现无需后端 API
2. **分享内容三要素** - 每次分享必须包含 `title`（标题）、`path`（落地页路径）、`imageUrl`（封面图），确保分享卡片完整
3. **分享菜单预注册** - 页面加载时调用 `wx.showShareMenu` 注册分享能力，同时开启好友和朋友圈两个通道
4. **动态分享内容** - 分享标题和图片根据当前页面上下文动态生成（如当前查看的目的地名称、生成的头像图片）

## 图片与资源管理

1. **云端存储** - 所有图片使用 ImageX（火山引擎）云存储 + CDN 分发，禁止将大图片打包进客户端
2. **多尺寸生成** - 头像等关键图片上传后自动生成多尺寸版本，按场景加载合适尺寸
3. **水印保护** - AI 生成的图片自动添加水印，保护内容版权
4. **启动预加载** - 应用启动时通过 `preloadCDNImages` 预加载关键 CDN 图片列表，使用 `wx.downloadFile` 提前缓存到本地

## AI 内容生成

1. **异步生成 + 轮询** - AI 生成任务通过 `BackgroundTasks` 异步执行，前端每 2 秒轮询检查生成状态
2. **生成状态机** - 生成任务使用 `pending → completed / failed` 状态机管理，前端根据状态展示对应 UI（加载中 / 结果 / 错误）
3. **过渡屏幕** - AI 生成等待期间展示过渡屏幕（transition-screen），用文案和动画填充等待时间，营造仪式感
4. **生成结果持久化** - 生成结果存储到用户 profile 的 JSON 字段（如 `generated_avatars`），支持历史查看和断点续传

## 支付流程设计

1. **支付前置校验** - 发起支付前校验用户登录状态、商品可用性、价格一致性
2. **幂等性保证** - 支付订单使用唯一订单号（`BT{timestamp}{random}` 格式），防止重复支付；回调处理必须幂等
3. **状态机管理** - 支付订单使用明确的状态机（pending → paid → completed / failed），每次状态变更记录日志
4. **优雅降级** - 支付失败区分"用户取消"和"系统错误"，给出不同的提示和引导
5. **回调签名验证** - 微信支付回调必须验证签名和金额一致性，防止伪造回调
6. **解锁状态查询** - 提供独立的 `/api/payment/unlocked` 接口查询用户已解锁的商品，前端据此控制内容展示

## 导航与路由

1. **自定义 TabBar** - 使用自定义 TabBar 组件替代原生 TabBar，支持品牌化设计和动画效果
2. **步骤路由映射** - 多步骤流程使用 `STEP_ROUTES` 映射表管理步骤与页面的对应关系，集中维护流程顺序
3. **断点续传路由** - 用户进入应用时，根据 profile 中的步骤字段（如 `generated_avatars[0].step`）自动路由到上次中断的位置
4. **统一导航函数** - 通过 `utils/navigation.ts` 提供统一的导航工具函数，封装 `wx.navigateTo` / `wx.redirectTo` 等调用

## 分页与列表

1. **双模式分页** - 支持 `offset + limit`（无限滚动场景）和 `page + limit`（翻页场景）两种分页模式，按业务场景选用
2. **hasMore 标志** - 分页响应必须包含 `hasMore`（或 `hasNext` / `hasPrev`）标志，前端据此控制加载更多的触发和展示
3. **滚动触底加载** - 列表页使用滚动位置检测触发下一页加载，配合 `loadingMore` 状态防止重复请求
4. **加载状态分离** - 区分首次加载（`loading`）、加载更多（`loadingMore`）、下拉刷新（`refreshing`）三种状态，展示不同的 UI 反馈

## 用户资料

1. **Profile 聚合** - 用户资料通过 `/api/auth/me` 单一接口返回聚合数据（基本信息、护照、生成头像等），减少多次请求
2. **JSON 字段扩展** - 用户的动态数据（照片列表、生成头像、技能等）使用 JSON 字段存储，避免频繁加表，保持 schema 灵活性
3. **分步收集** - 用户信息按流程分步收集（登录时收集昵称头像 → 后续收集生日 → 使用中积累照片和生成结果），不在注册时要求填写所有信息
4. **多端同步** - 用户数据以后端为单一数据源，各端通过 API 获取最新数据，本地仅缓存用于快速展示

## 积分与激励

1. **积分日志** - 积分变动通过独立的 `zha_point_log` 表记录，包含变动原因和数值，支持审计和追溯
2. **余额冗余** - 用户实体维护 `zha_point` 余额字段，每次变动同步更新，避免实时聚合计算

## API 设计

1. **RESTful 语义** - HTTP 方法语义化：GET 查询、POST 创建、PUT 更新、DELETE 删除
2. **统一响应格式** - 所有 API 返回统一的数据结构，错误响应包含明确的错误码和用户友好的错误信息
3. **认证分层** - 区分公开接口、登录用户接口、管理员接口，使用依赖注入实现权限控制
4. **后台任务** - 耗时操作（AI 生成、图片处理）使用后台任务异步处理，前端轮询获取结果

## 数据模型设计

1. **基类复用** - 实体模型继承统一基类，自动管理主键、创建时间、更新时间
2. **软删除** - 使用 `archived` 字段标记删除，保留数据可追溯性，禁止物理删除用户数据
3. **索引优化** - 高频查询字段添加索引，唯一约束字段使用唯一索引，关联查询字段使用联合索引
4. **类型安全** - 使用 ORM 的类型系统定义字段，配合 Pydantic 模型做请求/响应的类型校验
5. **种子数据真实性** - 预置数据（seed data）中涉及书名、作者、引文等事实性内容必须经过验证，禁止凭印象编造；书名与作者必须匹配，引文必须出自声称的来源
6. **种子数据可更新** - seed 脚本应支持增量更新（不仅是首次导入），或提供独立的更新脚本；跳过已有数据的逻辑不应阻碍后续的数据修正
