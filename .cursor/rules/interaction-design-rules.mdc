---
description: 交互设计最佳实践，涵盖动画系统、手势反馈、过渡设计、加载状态、错误处理等通用规则
globs:
alwaysApply: true
---

## 动画系统

1. **动画分级** - 动画按用途分级管理：微交互（< 0.3s）、过渡动画（0.3-0.6s）、装饰动画（持续循环）；不同级别使用不同的时长和缓动函数
2. **时长克制** - 过渡动画控制在 0.25s-0.65s，避免拖沓
3. **缓动函数选择** - 使用自定义 cubic-bezier 而非 linear/ease，赋予物理质感；进入动画用 `ease-out`，退出动画用 `ease-in`，循环动画用 `ease-in-out`；弹性效果用 `cubic-bezier(0.34, 1.56, 0.64, 1)`
4. **方向语义** - 切换动画方向与操作方向一致（向下翻 → fadeUp，向左翻 → slideLeft）
5. **动画延迟编排** - 多元素依次出现时使用递增的 `animation-delay`，营造层次感和节奏感
6. **呼吸动画** - 需要吸引注意力的元素使用呼吸动画（scale + opacity 循环），频率 4-6 秒一个周期
7. **淡入动画** - 页面内容加载完成后使用 `fade-in-up`（opacity + translateY）进入，避免内容突然出现
8. **性能优先** - 动画只使用 `transform` 和 `opacity` 属性，避免触发重排（layout）的属性动画
9. **减弱动画适配** - 尊重 `prefers-reduced-motion`，降级为纯 opacity 短时过渡

## 过渡设计

1. **页面过渡** - 页面切换使用过渡屏幕（transition-screen），承载等待时间并营造仪式感
2. **模态框过渡** - 模态框进入使用 `scale(0.9) + translateY + opacity` 组合动画，退出使用反向动画
3. **状态过渡** - 元素状态变化（显示/隐藏、启用/禁用）必须有过渡动画，禁止瞬间切换
4. **自动推进** - 过渡页面设置合理的自动推进时间（3-5 秒），同时提供手动跳过选项

## 手势与反馈

1. **触摸反馈** - 所有可交互元素必须有触摸反馈：按下时 `scale(0.95-0.98)` 或改变透明度/阴影变化
2. **防重复点击** - 关键操作（支付、提交）使用 `isAnimating` 或 `isLoading` 标志防止重复触发
3. **操作确认** - 不可逆操作（删除、支付）必须有二次确认弹窗
4. **即时反馈** - 用户操作后 100ms 内必须给出视觉反馈，长时间操作立即显示加载状态
5. **边界反馈** - 到达列表边界时给出回弹/抖动等微反馈，而非静默无响应
6. **多通道输入** - 同一操作支持多种触发方式（滑动 / 键盘 / 按钮），覆盖不同设备和习惯
7. **手势节流** - 滚轮/滑动等连续手势需加节流锁（400-600ms），防止过度触发
8. **物理隐喻** - 用翻转、回弹、缩放等拟物动效增强操作的直觉感
9. **视觉暗示优先于文字提示** - 操作引导优先使用视觉暗示（露出相邻卡片边缘、浮动箭头、呼吸光弧）而非文字说明（如"上下滑动切换"）；文字提示打破沉浸感，视觉暗示让用户通过直觉发现操作
10. **首次引导自动消失** - 操作引导提示（箭头、浮动指示器）仅在首次进入时显示，用户完成首次对应操作后立即隐藏，不持续干扰；使用状态标志位控制
11. **分阶段渐进引导** - 多维度操作（如上下滑+左右滑+点击）的引导提示必须分阶段出现，每次只引导一个操作；用户完成当前操作后再展示下一个引导，避免信息过载；例如：首屏只提示下滑 → 用户滑动后提示左右切换 → 用户切换后提示消失
12. **先出 HTML Demo 再上小程序** - 涉及视觉效果（动画、渐变、特效）的交互设计，必须先用纯 HTML/CSS 做 Demo 确认效果，再移植到小程序；直接在小程序中试错成本高（Skyline 限制多、构建-编译-预览周期长），HTML Demo 可以快速迭代
13. **Worklet 动画避免分段 setTimeout** - Skyline Worklet 动画中，避免将一个连贯动画拆成多段 setTimeout + setData + 重新绑定 applyAnimatedStyle 的链式调用，会导致中间帧卡顿；应让正反面同时存在 DOM，用一个 shared 值 + 一次 timing 调用驱动完整动画
14. **CSS 技巧先搜索最佳实践** - 实现 CSS 视觉效果（文字截断渐隐、遮罩、溢出处理等）前，先搜索业界最佳实践（如 `mask-image` 渐隐截断、`-webkit-line-clamp`、`flex-end` 对齐露出末行），避免用 hacky 方案（伪元素覆盖、direction:rtl 等）反复返工

## 加载状态设计

1. **加载分级** - 全屏加载（页面级）、局部加载（区域级）、内联加载（按钮级）三种模式，根据场景选择
2. **骨架屏** - 首次加载使用骨架屏占位，避免空白页面；骨架屏形状应与实际内容布局一致
3. **加载文案** - 加载状态配合有意义的文案（如"正在为你生成..."而非"加载中..."），减少用户焦虑
4. **轮询策略** - 异步任务使用轮询获取结果，间隔 2-3 秒，设置最大轮询次数，超时后提示用户

## 错误处理与反馈

1. **错误分级** - 轻量错误用 Toast 提示（自动消失），重要错误用 Modal 确认（需用户操作），致命错误引导用户重试
2. **友好错误信息** - 错误提示使用用户能理解的语言，禁止直接展示技术错误信息（如 HTTP 状态码、堆栈信息）
3. **权限引导** - 权限被拒绝时，提供明确的引导（如"请前往设置开启相册权限"），并提供快捷跳转
4. **网络异常** - 网络请求失败时区分"无网络"和"服务器错误"，给出不同的提示和重试策略

## 成功反馈

1. **仪式感** - 关键成功节点（支付成功、生成完成）使用丰富的视觉反馈（烟花、粒子、弹性缩放），营造成就感
2. **奖励反馈** - 积分获取、成就解锁等奖励使用专属的 Toast 组件，包含动画和详细信息
3. **渐进展示** - 结果页面内容分批展示（先标题、再内容、最后操作按钮），配合动画延迟营造揭晓感

## 空状态设计

1. **引导性空状态** - 空状态页面必须包含引导文案和操作按钮，告诉用户下一步该做什么
2. **占位图** - 数据未加载或为空时使用默认占位图，保持页面结构完整
3. **区分状态** - 区分"数据为空"（引导创建）和"加载失败"（引导重试）两种空状态
