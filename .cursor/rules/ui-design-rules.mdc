---
description: UI 设计最佳实践，涵盖色彩系统、组件设计、布局模式、视觉风格、设计系统等通用规则
globs:
alwaysApply: true
---

## 呈现模式

1. **网页端（Desktop/Web）** - 用于 Demo 展示、管理后台、落地页等场景；采用响应式布局，居中展示内容区域，背景可做氛围渲染（渐变、粒子、网格纹理）；操作区域放在页面顶部或侧边栏
2. **移动端模拟（iPhone Shell）** - 用于在网页中预览小程序/APP 效果；外层包裹 iPhone 外壳（390×844px, border-radius: 56px），内含 Dynamic Island 和 Home Indicator 仿真；`@media (max-width: 430px)` 时去掉外壳，退化为真机全屏模式
3. **模式选择原则** - 面向用户的阅读体验页面优先使用移动端模拟模式，管理后台和数据展示页面使用网页端模式；同一项目中两种模式可并存，但单个页面只选一种
4. **外壳与内容分离** - iPhone Shell 仅作为展示容器，内部 APP 内容独立渲染，不依赖外壳的样式；真机访问时外壳自动隐藏，内容无缝适配全屏

## 视觉层级

1. **聚焦优先** - 页面核心内容占据视觉最高权重，其余元素通过透明度/尺寸/颜色做减法
2. **透明度梯度** - 用 3-4 级透明度区分信息层级（主体 → 辅助 → 弱化 → 极淡），避免用颜色数量堆砌层次
3. **字号对比** - 焦点内容与上下文之间保持明显的字号差（至少 2-4px），配合 font-weight 变化强化主次；展开/折叠状态切换时焦点内容的字体属性（字号、行高、字重）必须保持完全不变，仅通过显隐辅助内容实现状态切换

## 色彩系统（通用）

1. **语义化命名** - 颜色使用语义化命名（primary、secondary、accent、destructive），禁止在组件中直接使用色值；通过 Tailwind 配置或 CSS 变量统一管理
2. **色彩分层** - 色彩体系按用途分层：背景色（多级深浅）、强调色（品牌色系）、功能色（成功/警告/错误）、中性色（文字/边框/分割线）
3. **透明度体系** - 使用统一的透明度梯度（0.08 / 0.12 / 0.20 / 0.30）控制层次感，避免随意设置透明度值
4. **CSS 变量驱动** - 管理后台等 Web 端使用 CSS 变量定义主题色，至少包含：背景、卡片背景、主文字、次要文字、弱化文字、强调色、分割线；支持运行时切换主题
5. **强调色克制** - 全局只用一个强调色，通过不同透明度派生出 muted / dim 变体

## 组件设计

1. **玻璃态卡片** - 卡片组件使用半透明背景 + 模糊边框 + 内阴影，营造层次感和精致感
2. **按钮层级** - 按钮分为主按钮（渐变填充）、次按钮（半透明填充）、幽灵按钮（仅边框），每个页面最多一个主按钮
3. **毛玻璃按钮** - 交互按钮使用半透明背景 + 细边框，轻量不抢焦点；Web 端可加 `backdrop-blur`，**小程序端（Skyline）禁止使用 `backdrop-blur`**，改用纯半透明背景色
4. **统一圆角** - 通过 Tailwind 配置统一圆角值（如 card: 20px, card-lg: 28px），禁止在组件中使用任意圆角值；主要内容卡片使用较大圆角（16-32px），营造柔和的视觉感受
5. **阴影层级** - 阴影分为小（sm）、中（DEFAULT）、大（lg）三级，带品牌色调的阴影增强视觉统一性
6. **进度指示** - 用圆点/胶囊等微型指示器展示当前位置，active 态通过宽度和颜色变化区分
7. **提示自消失** - 操作提示在用户首次交互后自动隐藏，不持续干扰
8. **Headless 组件** - Web 端 UI 组件基于 Headless 库（如 Radix UI）构建，用 Tailwind 自定义样式，通过 CVA 管理变体
9. **组件变体** - 使用 Class Variance Authority (CVA) 定义组件变体（variant + size），通过 `cn()` 工具函数合并类名

## 布局模式

1. **全屏沉浸** - C 端页面采用全屏沉浸式布局，隐藏系统导航栏，自定义顶部和底部；小程序端使用 `style="height:100vh;"` 而非 Tailwind 的 `h-screen` class（Skyline 下更可靠）
2. **安全区域** - 必须适配安全区域（`env(safe-area-inset-*)`），通过 Tailwind spacing 扩展统一管理，适配刘海屏和底部手势条
3. **Flex 优先** - 布局优先使用 Flexbox（`flex flex-col`、`flex flex-wrap`），禁止使用 Grid（Skyline 不支持）
4. **居中模式** - 内容居中统一使用 `flex items-center justify-center`，避免使用 margin auto 或绝对定位居中
5. **响应式网格** - 管理后台使用响应式网格布局（`grid-cols-2 md:grid-cols-3 lg:grid-cols-4`），移动端优先
6. **内容最大宽度** - 阅读内容设置 max-width（600-800px），保证长文本的可读性
7. **移动端适配** - 桌面端和移动端的操作区域分别处理（桌面顶部工具栏，移动端底部浮动栏）
8. **Skyline 固定元素** - 小程序 Skyline 不支持 `position:fixed`；需要固定在底部的元素（如导航栏）应作为页面 flex 布局的末尾子元素，而非使用固定定位
9. **卡片固定尺寸** - 类 TikTok 的全屏卡片场景中，卡片必须使用固定高度（如 `height:480px`），内容区 `overflow:hidden` 裁切，确保无论内容多少卡片大小不变
10. **Swiper 露边效果** - 使用 swiper 实现卡片轮播时，通过 `previous-margin` + `next-margin` 露出相邻卡片边缘暗示可滑动；swiper-item 内部必须有 padding（水平 8-12px、垂直 6-10px）制造间隙，否则相邻卡片会被完全遮住；相邻卡片的 opacity 不低于 0.6、border 不低于主卡片，确保边缘在深色背景上可辨识
11. **滑动优先于手势模拟** - 需要上下/左右翻页的场景优先使用 swiper 组件（自带平滑过渡和 snap 吸附），而非手动监听 touch 事件模拟；手动手势代码复杂且在 Skyline 下行为不稳定

## 文字排版（通用）

1. **字体层级** - 定义明确的字体层级：display（展示）、title（标题）、body（正文）、whisper（辅助），每级包含字号、行高、字距
2. **字距控制** - 中文排版使用 0.02-0.1em 的字距，标题字距大于正文，营造呼吸感
3. **行高设置** - 正文行高 1.8-2.0 倍，标题行高 1.2-1.4 倍，保证阅读舒适度
4. **文字截断不切字** - `overflow:hidden` + `max-height` 截断文本时，`max-height` 必须是 `line-height` 的整数倍（如 `14px * 1.9 = 26.6px`，取 27px），不能切到半个字；Web 端优先使用 `lh` 单位自适应
5. **文字渐隐截断** - 需要截断文本并暗示"还有更多"时，使用 `mask-image: linear-gradient()` 让末尾几个字从实色渐变到透明（Web 端）；渐隐方向跟随阅读方向：截掉后续内容时右侧渐隐，截掉前置内容时左侧渐隐
6. **露出末行而非首行** - 需要展示一段文字的结尾部分（如上文最后一行紧挨核心内容）时，使用 `display:flex; flex-direction:column; justify-content:flex-end` + `overflow:hidden` + `max-height` 让内容底部对齐、顶部截断

## 视觉风格（三选一）

以下三种风格覆盖色彩氛围、字体选择、特效手法，项目启动时选定一种，全局统一执行。

### 风格 A：墨纸书卷（Ink & Paper）

- **气质** - 温暖、克制、文学感，像翻开一本旧书
- **背景** - 深棕/墨色系（`#1E1D1B`），卡片用略浅的棕灰（`#2A2926`），层次靠明度微差而非透明度
- **强调色** - 暖铜色（`#D4956E`），通过 15% / 8% 透明度派生 muted / dim 变体
- **文字** - 主文字暖白（`#EBE1D2`），四级梯度递减至深棕（`#4A453F`）；标题使用宋体系（Noto Serif SC / Songti SC）营造文学感，正文使用黑体系（PingFang SC）保证可读性
- **特效** - 极度克制，不使用发光/粒子/模糊；装饰仅限线性渐变分割线（`transparent → accent → transparent`）和卡片 3D 翻转；按压反馈用 `scale(0.985)` + 阴影收缩
- **卡片** - 实色填充 + 细边框（`rgba(235,225,210,0.06)`）+ 双层投影，禁止 backdrop-blur

### 风格 B：星空玻璃（Cosmic Glass）

- **气质** - 科技、梦幻、沉浸感，像漂浮在深空中阅读
- **背景** - 近黑蓝（`#070912`），多层大尺寸模糊圆形（`blur 90-140px`，白色 6-10% 透明度）叠加径向渐变，最底层加 42px 网格纹理（白色 5% 透明度）
- **强调色** - 纯白，通过透明度梯度（`white/80` → `white/10`）构建全部层次，不引入彩色
- **文字** - 纯白体系，焦点文字 100% 白 + `text-shadow` 微发光，辅助文字递减至 `white/40`；字体使用系统无衬线（SF Pro / PingFang SC）
- **特效** - 毛玻璃卡片（`backdrop-blur-xl` + `bg-white/8` + `border-white/15`）；焦点行附带 `bg-white/10` 高亮背景块；入场动画带 `blur(6px)` 渐显
- **粒子/发光** - 可选装饰性星尘粒子（绝对定位 + 随机位置 + 随机延迟），关键元素 `box-shadow` 多层发光

### 风格 C：灵动暗彩（Playful Dark）

- **气质** - 趣味与高级感并存，像雨后深夜的竹林咖啡馆，暗而通透、精致又灵动
- **背景** - 深青黑（`#0A0F0E`），叠加低饱和彩色光斑（翠绿 `#34D399/5.5%`、琥珀 `#F0A050/4%`、青 `#34D399/3.5%`）；Web 端可用 `blur 120px` + 漂移动画，**小程序端（Skyline）禁止使用 `filter:blur()`**，改用纯色低透明度大圆形模拟光斑效果
- **强调色** - 渐变双色系：主色翠绿（`#34D399`）→ 辅色琥珀金（`#F0A050`），按钮和焦点元素使用 135° 渐变填充；非交互场景退化为翠绿单色 + 透明度变体
- **文字** - 主文字冷薄荷白（`#E8F0EC`），辅助文字青灰（`#9AADA2`），弱化文字深青灰（`#546E64`）；字体使用圆润无衬线体（Noto Sans SC / SF Pro Rounded），标题 `font-weight: 600` 配合微量字距
- **特效** - 焦点文字带翠绿微光（`text-shadow: 0 0 28px rgba(52,211,153,0.15)`）；页面切换使用弹性位移动画；**小程序端禁止使用 CSS 3D 变换（`transform-style: preserve-3d`、`backface-visibility`）**，卡片翻面用 `wx:if` 条件渲染切换正反面
- **卡片** - 半透明深色填充（`rgba(255,255,255,0.035)`）+ 1px 渐变边框（翠绿→琥珀，10% 透明度）+ `box-shadow` 投影；**小程序端禁止使用 `backdrop-blur`**，Web 端可用；active 态边框亮度提升至 25%，投影扩散

## 图片与资源

1. **CDN 托管** - 所有图片资源使用 CDN 或云存储托管，禁止将大图片打包进应用
2. **多尺寸适配** - 图片提供多尺寸版本（缩略图、标准、高清），根据场景加载合适尺寸
3. **等比裁剪** - 列表中的图片使用固定宽高比（`aspect-square`、`aspect-video`），保持视觉一致性
4. **图片预加载** - 关键路径上的图片提前预加载，避免用户看到加载过程

## 图标系统

1. **统一图标库** - 使用统一的图标库（如 Lucide React），保持图标风格一致
2. **统一尺寸** - 图标使用统一的尺寸规范（如 `h-4 w-4`、`h-5 w-5`），与文字大小匹配
3. **TabBar 图标** - 底部导航图标提供选中/未选中两套，选中态使用品牌色

## 视觉可见性

1. **透明度下限** - 装饰性元素的 opacity 不得低于 0.1，否则在真实屏幕上不可见；SVG stroke 的 opacity 不得低于 0.15
2. **先看后调** - 视觉效果必须在真实设备或模拟器上确认可见后才算完成，不能凭代码中的数值想象效果
3. **UI 改版不丢功能** - 重写页面 UI 时，必须逐一对照原版所有功能元素（按钮、手势区域、条件显示逻辑），确保零遗漏；建议先列出功能清单再动手
4. **卡片形态不可丢失** - swiper 卡片式页面中，卡片容器（圆角、背景色、阴影）和 swiper 的 margin（previous-margin / next-margin）是核心交互暗示，UI 改版时必须保留

## 设计系统维护

1. **Token 化** - 所有设计值（颜色、间距、圆角、阴影、字体）在 Tailwind 配置中统一定义为 Design Token
2. **禁止魔法值** - 禁止在组件中使用未定义的颜色值、间距值、圆角值，所有值必须来自设计系统
3. **组件文档** - 基础 UI 组件（Button、Card、Input、Toast）放在独立的 `components/ui/` 目录，保持通用性和可复用性
4. **类名工具** - 使用 `cn()` 函数（clsx + tailwind-merge）合并类名，支持条件类名和覆盖默认样式
